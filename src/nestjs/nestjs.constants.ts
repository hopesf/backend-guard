/**
 * Injection token for BackendGuardOptions in the NestJS DI container.
 * Used by guards and interceptors to access the configured options.
 */
export const BACKEND_GUARD_OPTIONS = "BACKEND_GUARD_OPTIONS";
